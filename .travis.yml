language: node_js

node_js:
  - 10

script:
  - npm install
  - npm run build
  - cp public/* dist/

deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  local-dir: dist/
  target-branch: gh-pages
  on:
    branch: master
